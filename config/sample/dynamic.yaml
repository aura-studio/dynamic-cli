environments:
  - name: default
    toolchain:
      os: linux
      arch: amd64
      compiler: go1.22.1
      variant: plain
    warehouse:
      local: /tmp/warehouse
      remote:
        - s3://mirroring-lambda
procedures:
  - name: brazil
    environment: default
    source:
      module: codeup.aliyun.com/mirror/admin/admin-api
      package: route
      version: brazil
    target:
      namespace: admin
      package: route
      version: latest
